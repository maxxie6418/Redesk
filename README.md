<div align="center">

# 📚 Redesk

**自托管的个人阅读管理与 AI 陪读系统**

> 把书库、阅读痕迹、主题思考沉淀在自己手里，让 AI 基于你真实的阅读经历陪读、提问、归纳。

[![Status](https://img.shields.io/badge/status-M1%20书架功能-green.svg)](#项目状态)
[![Stack](https://img.shields.io/badge/stack-TypeScript-3178c6.svg)](#技术栈)

</div>

---

## 项目介绍

Redesk 是一个以书籍为入口、以阅读经历为核心资产、以 AI 协同利用为长期方向的**个人知识书库系统**。

区别于单纯的书单管理工具、以阅读器体验为核心的电子书应用、或仅用于暂存文件的资源仓库，Redesk 把以下内容纳入同一套可长期积累、可持续调用的体系：

- 书籍及其元数据
- 书籍资源文件
- 阅读过程中的行为痕迹（高亮、进度）
- 阅读过程中形成的思考痕迹（笔记、判断）
- 后续可供 AI 利用的个人知识背景

### 三层架构

产品由三个层次构成，主次明确，阶段开发不得反向改变：

| 层级 | 名称 | 定位 |
| :---: | --- | --- |
| 基础层 | 书架管理 | 书籍与文件的稳定管理，长期收藏、筛选、状态流转 |
| 沉淀层 | 阅读与笔记 | 承担数据生产，把高亮、笔记、判断沉淀为可复用资产 |
| 增强层 | AI 知识书库 | 长期核心方向，基于个人阅读经历提供陪读、归纳、问答 |

### 核心价值

Redesk 的长期价值在于能否把个人阅读中真正有价值的内容长期保存并组织起来，而不在于支持的格式数量或阅读器的细节能力：

- 我拥有哪些书，我为什么保留这些书
- 我准备读什么、正在读什么、读过什么、放弃了什么
- 我在阅读中关注了哪些内容，留下了哪些判断、问题、启发
- 这些内容未来如何被自己再次使用，如何被 AI 在我的语境下调用

### 数据主权

数据存于本地 SQLite，自托管 Docker 部署，不默认强依赖云端。核心数据支持导出（Markdown / JSON / CSV）与全量备份，不绑架你的数据——即使有天 Redesk 不在了，你的笔记用任何文本编辑器都能打开。

---

## 🚧 项目状态

**M0 工程地基已搭建，M1 书架功能大部分已落地（v1.0.0）。**

已完成的文档体系（位于 `doc/`）：

| 层级 | 文档 | 说明 |
| :---: | --- | --- |
| 产品层 | [需求总纲](doc/需求总纲.md) | 产品定义、边界、阶段原则 |
| 产品层 | [功能清单 v1.2.2](doc/Redesk-功能清单-v1.1.md) | 全量功能点（编号 1.x–10.x） |
| 产品层 | [决策记录](doc/决策记录.md) | 已确认决策与暂缓问题 |
| 架构层 | [技术方案 v1.1.2](doc/技术方案.md) | 技术路线、技术栈、分项设计、分期 |
| 实现依据层 | [数据模型 v1.0.2](doc/数据模型.md) | 全量 schema、ER、索引、检索结构 |
| 实现依据层 | [API 接口 v1.0.2](doc/API接口.md) | 通用约定、Step1 端点、预留端点 |
| 执行层 | [开发执行计划 v1.0.2](doc/开发执行计划.md) | 六里程碑、按序执行项、功能点覆盖 |
| Review 辅助 | [数据模型导览](doc/数据模型导览.html) | 面向非技术视角的字段说明（浏览器打开） |

当前已具备：

- pnpm monorepo：`apps/api`、`apps/web`、`packages/db`、`packages/shared`
- Fastify 后端：健康检查、配置加载、CORS、统一错误处理、鉴权接口、Zod 校验
- Drizzle + better-sqlite3：S1 全套表迁移（books/book_files/categories/tags/book_tags/book_relations/status_history/settings）、FTS5 全文检索、启动时自动迁移
- Vite + React 前端：路由、Query、主题壳、侧边栏、书架页、书籍详情页、书库文件页、设置页、概览页、登录/初始化页
- 书架核心：书籍 CRUD（全字段录入）、multipart 文件上传、分类/标签/评分、五状态体系、搜索/筛选/排序/分页、批量操作、回收站、状态历史
- 文件管理：一书多文件、主阅读文件、文件下载（Range）、替换/删除、EPUB 封面抽取、书库文件视图（未关联文件池、checksum 去重）
- 导出与备份：元数据 JSON/CSV 导出、全量 ZIP 备份、SQLite 自动备份
- OPDS 1.2 输出（KOReader 兼容）
- 本地启动脚本：`start-local.bat` / `start-local.ps1`
- Dockerfile 与 docker-compose 基础部署文件

说明：当前本地开发模式临时免登录，便于先推进书架功能；后端鉴权链路仍保留，生产构建默认不免登录。

---

## 🗺️ Roadmap

当前进展：M0 工程地基已搭建。下方按功能领域列出规划功能与状态。

> 状态：🗓️ 规划中 / 👷 开发中 / ✅ 已上线

| 功能领域 | 功能 | 状态 |
| --- | --- | :---: |
| **书库管理** | 书籍录入与元数据管理（全字段、multipart 上传） | ✅ 已上线 |
| | 文件上传与多格式存档（EPUB / PDF / MOBI / TXT / AZW3 等） | ✅ 已上线 |
| | 书库文件视图（未关联文件池、checksum 去重、匹配书籍） | ✅ 已上线 |
| | 分类（个人/常规双类型）、标签、评分 | ✅ 已上线 |
| | 五状态体系（收录 / 计划读 / 在读 / 已读 / 存） | ✅ 已上线 |
| | 搜索、筛选、批量操作 | ✅ 已上线 |
| | 回收站与状态变更历史 | ✅ 已上线 |
| | 重复检测（规则版） | ✅ 已上线 |
| | 书籍关联关系管理 | ✅ 已上线 |
| | 收藏（favorite） | ✅ 已上线 |
| **在线阅读** | EPUB 浏览器内阅读 | 🗓️ 规划中（M2） |
| | 高亮划线与批注 | 🗓️ 规划中（M2） |
| | 阅读进度记忆 | 🗓️ 规划中（M2） |
| | 跳回原文定位 | 🗓️ 规划中（M2） |
| | 独立笔记（富文本） | 🗓️ 规划中（M2） |
| **主题阅读** | 跨书主题组织与关联 | 🗓️ 规划中（M4） |
| | 主题内高亮 / 笔记 / 章节片段汇总 | 🗓️ 规划中（M4） |
| | 主题沉淀（待探索问题 / 阶段判断 / 多书比较） | 🗓️ 规划中（M4） |
| **AI 陪读** | 录入辅助（元数据补全 / 标签 / 重复发现） | 🗓️ 规划中（M3） |
| | 阅读辅助（摘要 / 问题整理，带引用） | 🗓️ 规划中（M3） |
| | 基于个人书库的语义问答（RAG） | 🗓️ 规划中（M4） |
| | AI 分级访问控制 | 🗓️ 规划中（M5） |
| **数据主权与集成** | 导出（JSON / CSV） | ✅ 已上线 |
| | 全量备份与自动备份 | ✅ 已上线 |
| | OPDS 订阅（KOReader 等阅读器） | ✅ 已上线 |
| | 对象存储云同步（S3 / OSS） | 🗓️ 规划中（M5） |
| **其他** | 自托管 Docker 部署 | ✅ 已上线 |
| | 单账户登录与多用户预留 | ✅ 已上线 |
| | 暗色模式 / 设置页 / 概览首页 | ✅ 已上线 |
| | 阅读器增强项（主题 / 字体 / TTS 等，待定位） | 🗓️ 规划中（M5） |
| | 阅读统计增强（热力图 / 趋势等，待定位） | 🗓️ 规划中（M5） |

详细的开发排期见 [开发执行计划](doc/开发执行计划.md)。

---

## 🛠️ 技术栈

技术路线：**全栈 TypeScript（Route A）**。选型主因为"选错最好补救"——AI 形态未定型时，可加 Python RAG 边车而不推倒主应用。

| 类别 | 技术选型 |
| --- | --- |
| 后端 | Node.js + Fastify + better-sqlite3 + Drizzle ORM |
| 前端 | Vite + React + TypeScript + shadcn/ui |
| 存储 | SQLite 单文件（FTS5 全文检索 + sqlite-vec 向量检索） |
| 阅读器 | epub.js（CFI 锚定进度与高亮） |
| 笔记 | TipTap 富文本（HTML + Markdown 双存） |
| 部署 | 自托管 Docker（docker-compose） |
| AI | 外部 LLM API，`AIService` 接口隔离，分阶段引入 |

---

## 📁 项目结构

```
Redesk/
├── apps/
│   ├── api/          # Fastify 后端
│   └── web/          # Vite + React 前端
├── packages/
│   ├── db/           # Drizzle schema + better-sqlite3
│   └── shared/       # 前后端共享类型
├── doc/              # 全部设计文档
└── docker-compose.yml
```

---

## 🎯 核心设计原则

- **`存` 是独立状态，不是删除**：五状态之一，与回收站软删除是两条路径。
- **主题只引用不占有**：高亮/笔记归原书所有，主题是重组再利用层。
- **AI 内容与用户内容隔离**：AI 生成内容作为派生资产单独存放，不混入原始笔记。
- **数据主权**：导出用通用格式（Markdown/JSON），不依赖专有结构。
- **预留多用户**：用户拥有的根实体带 `owner_id`，从属关系表通过父表继承归属，当前单账户无感，未来可演进。

---

## 🚀 部署与开发

### 环境要求

- **Node.js**：>= 20
- **pnpm**：最新版（通过 `npm install -g pnpm` 安装）

### 本地启动

首次运行前，需在项目根目录安装依赖：

```bash
pnpm install
```

Windows 下可直接双击：

```text
start-local.bat
```

脚本会打开 API 与 Web 两个服务窗口，并在服务就绪后自动打开：

```text
http://localhost:5173/
```

也可以手动启动：

```bash
pnpm dev
```

常用命令：

```bash
pnpm dev          # 同时启动 API 与 Web
pnpm dev:api      # 仅启动后端，默认 http://localhost:8787
pnpm dev:web      # 仅启动前端，默认 http://localhost:5173
pnpm db:migrate   # 执行数据库迁移
pnpm typecheck    # 类型检查
pnpm lint         # ESLint 检查
pnpm build        # 生产构建
```

### 临时免登录

本地开发默认免登录：

```env
VITE_AUTH_DISABLED=true
```

如需恢复登录/初始化页，在 `.env` 中设置：

```env
VITE_AUTH_DISABLED=false
```

后端鉴权接口仍保留；生产构建默认不免登录。

### Docker

Docker Compose 运行前必须提供强随机 `SESSION_SECRET`，例如在 `.env` 中设置：

```env
SESSION_SECRET=your-long-random-secret
```

然后运行：

```bash
docker compose up -d
```

### 本地测试文件

以下目录用于本地测试素材或运行产物，已加入 `.gitignore`，不会提交到 GitHub：

- `local-tests/`
- `test-files/`
- `.uploads/`
- `data/`

---
