# 第一阶段修复交接

- 日期：2026-07-01
- 版本号：v1.5.3
- AI：GPT-5 Codex

## 本轮已完成

1. 密码修改链路已修正。
   - 普通改密要求校验当前密码。
   - 首次强制改密保留免输旧密码逻辑。
   - 前后端契约已统一为 `current_password` / `new_password`。

2. 存储三态主链路已修正。
   - 文件上传已按 `local_only / cloud_only / dual` 执行真实写入。
   - 远程封面抓取已按存储模式写入。
   - 文件下载已改为按主位置优先、另一侧兜底。
   - 文件匹配兼容接口 `/files/:id/match` 已补上。
   - 文件库分页接口 `/files` 已补上汇总信息 `summary`。

3. 系统设置 owner 作用域已修正。
   - 去掉了系统设置和存储配置里对 `owner_id = 1` 的硬编码。
   - 改为读取首个用户作为系统配置归属用户。

4. 书籍 owner 边界已补齐一部分。
   - 新建书籍时会校验个人分类、常规分类、标签是否属于当前用户。
   - 编辑书籍时会校验个人分类、常规分类、标签是否属于当前用户。
   - 书籍序列化时标签查询已补 `tags.owner_id` 过滤。
   - 多标签筛选已改为 AND 语义。

5. 批量操作一致性已补一部分。
   - 批量改状态会补 `started_at / finished_at`。
   - 批量改分类、常规分类、标签时已补 owner/type 校验。
   - 批量删除返回的 `affected` 已改为真实可删除数量。

6. 统计口径已补一部分。
   - `/overview` 已补 `favorite_count`。
   - `/system/stats` 已补 owner 过滤。
   - `/system/stats` 的 `storage_size_bytes` 已去掉本地目录与数据库文件大小的重复累计。

7. 前端类型链路已恢复。
   - `apps/api` `tsc --noEmit` 已通过。
   - `apps/web` `tsc --noEmit` 已通过。
   - `eslint apps/api/src apps/web/src --ext .ts,.tsx` 已通过，无 error，仅剩 warning。

## 当前还没做完

### P1

1. 概览页 UI 还没完全跟上后端。
   - `favorite_count` 已进入接口，但 `overview.tsx` 里 KPI 展示仍是旧文本/旧值。
   - 时间线、快捷入口里仍有部分跳转指向 `/`，没有全部改成目标页或详情页。
   - “导入笔记”按钮仍未接动作。

2. 侧边栏统计文案和口径还没同步。
   - 当前 lint 已过，但侧边栏“话题/收藏”展示层没有一起按产品口径统一收口。

3. `custom_attributes` 前端能力只完成了类型适配。
   - 编辑表单和详情页已能通过类型检查。
   - 但详情页/编辑页文案、展示方式、异常 JSON 处理还可以继续收口。

### P2

1. `settings.tsx` 还有一批 hook warning。
   - 主要是 `useCallback` 缺少 `showToast` 依赖。
   - 这批 warning 不阻断功能，但建议单独整理，避免后续继续堆积。

2. 两个已有 `react-refresh` warning 未处理。
   - `apps/web/src/components/theme-provider.tsx`
   - `apps/web/src/components/ui/button.tsx`
   - 这两个是旧问题，本轮未动。

3. 文件库/概览/书架页面仍有部分乱码文案。
   - 本轮以功能修复为先，没有全面做文案清洗。

4. 书架页仍未补真分页体验。
   - 当前大量使用 `page_size: 200`。
   - 分类/标签筛选源也还有继续优化空间。

## 建议下一步顺序

1. 先收口展示层。
   - 修 `apps/web/src/routes/overview.tsx`
   - 修 `apps/web/src/hooks/use-sidebar-stats.ts`
   - 清掉明显错误跳转和空按钮

2. 再清 `settings.tsx` warning。
   - 这一块文件较大，建议只做 warning 和乱码整理，不混入功能改动。

3. 最后补测试。
   - 优先给密码修改、dual 存储、批量状态、分类/标签 owner 校验补回归测试。

## 建议补测范围

1. 改密码。
   - 首次强制改密成功。
   - 普通改密缺少旧密码失败。
   - 普通改密旧密码错误失败。

2. 存储三态。
   - `local_only` 仅本地有路径。
   - `cloud_only` 仅远端有路径。
   - `dual` 本地和远端路径同时写入。
   - 主位置缺失时下载兜底成功。

3. 书籍 owner 边界。
   - 非本人分类不能写入。
   - 分类类型写错时报错。
   - 非本人标签不能写入。

4. 批量操作。
   - 批量 `set_status` 自动补时间。
   - 批量 `delete` 的 `affected` 与真实删除量一致。

## 当前验证结果

```text
apps/api: tsc --noEmit 通过
apps/web: tsc --noEmit 通过
eslint: 通过，无 error，仅 warning
```

## 当前剩余 warning

1. `apps/web/src/routes/settings.tsx`
   - 多个 `react-hooks/exhaustive-deps` warning

2. `apps/web/src/components/theme-provider.tsx`
   - `react-refresh/only-export-components`

3. `apps/web/src/components/ui/button.tsx`
   - `react-refresh/only-export-components`

## 说明

1. 本轮没有处理所有展示层细节，主要先确保第一阶段闭环不再被核心链路问题卡住。
2. 本轮没有新增自动化测试文件，只完成了功能修复和静态检查验证。
3. 工作区原有改动较多，修复过程尽量避开了与现有 UI 调整的直接冲突。
